// This is a skeleton starter React page generated by Plasmic.
// This file is owned by you, feel free to edit as you see fit.
import * as React from "react";
import { PageParamsProvider as PageParamsProvider__ } from "@plasmicapp/react-web/lib/host";
import GlobalContextsProvider from "../components/plasmic/blank_project/PlasmicGlobalContextsProvider";

import { TranslationContextProvider } from "../components/plasmic/\u96EA\u9686\u5C11\u6797\u6587\u5316\u534F\u4F1A/PlasmicGlobalVariant__Translation";
import {
  Plasmic关于我们,
  Head
} from "../components/plasmic/\u96EA\u9686\u5C11\u6797\u6587\u5316\u534F\u4F1A/Plasmic\u5173\u4E8E\u6211\u4EEC";
import type { PageProps } from "gatsby";
export { Head };

function 关于我们({ location, path, params }: PageProps) {
  // Use Plasmic关于我们 to render this component as it was
  // designed in Plasmic, by activating the appropriate variants,
  // attaching the appropriate event handlers, etc.  You
  // can also install whatever React hooks you need here to manage state or
  // fetch data.
  //
  // Props you can pass into Plasmic关于我们 are:
  // 1. Variants you want to activate,
  // 2. Contents for slots you want to fill,
  // 3. Overrides for any named node in the component to attach behavior and data,
  // 4. Props to set on the root node.
  //
  // By default, Plasmic关于我们 is wrapped by your project's global
  // variant context providers. These wrappers may be moved to
  // Gatsby "wrapRootElement" function
  // (https://www.gatsbyjs.com/docs/reference/config-files/gatsby-ssr#wrapRootElement).

  return (
    <TranslationContextProvider value={undefined}>
      <GlobalContextsProvider>
        <PageParamsProvider__
          route={path}
          params={params}
          query={Object.fromEntries(new URLSearchParams(location.search))}
        >
          <Plasmic关于我们 />
        </PageParamsProvider__>
      </GlobalContextsProvider>
    </TranslationContextProvider>
  );
}

export default 关于我们;
